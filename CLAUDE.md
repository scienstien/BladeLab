# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

TurboDesigner 2.0 - appears to be a new project in initial setup phase.

## Environment Setup

- **Python Version**: 3.10.5 (defined in `venv/pyvenv.cfg`)
- **Virtual Environment**: `venv/` directory
- **Environment Variables**: `.env` file contains Hugging Face API token

## Directory Structure

- `api/` - API-related code (to be implemented)
- `env/` - Environment configuration (to be implemented)
- `test/` - Test files (to be implemented)
- `venv/` - Python virtual environment (gitignored)

## Notes

This is a new repository without any commits yet. Directory structure suggests a Python-based project with API functionality, possibly integrating with Hugging Face services.
