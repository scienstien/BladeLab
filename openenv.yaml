name: turbodesigner2
description: Physics-driven compressor optimization environment for turbomachinery design tasks.
domain: turbomachinery_design
observation_schema:
  title: Observation
  schema: env.models.Observation
action_schema:
  title: Action
  schema: env.models.Action
tasks:
  - feasibility
  - target_pr
  - target_pr_efficiency
max_episode_length: 30
